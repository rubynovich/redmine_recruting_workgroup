= form_for( :workload_competence, { url: { action: 'create' }, html: { multipart: true } } ) do |f|
  = hidden_field_tag( "project_id", params[:project_id] )
  = render partial: 'form', locals: {f: f}
  %div{align: "center"}
    = f.submit l(:button_create)
