-#
  .contextual
    = link_to l(:label_competence_new), {action: 'new'}, class: 'icon icon-add'

  %h2=l(:label_competence_plural)
  - html_title l(:label_competence_plural)

  #competence-list
    = render partial: 'competences'

  #competence-new
    = labelled_form_for @object, :url => {:action => :create} do |f|
      = render partial: 'form', locals: { f: f }
      = submit_tag l(:button_create)
      = submit_tag l(:button_create_and_continue), name: 'continue'
